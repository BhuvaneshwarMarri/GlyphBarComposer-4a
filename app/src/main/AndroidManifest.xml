<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="com.nothing.ketchum.permission.ENABLE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".GlyphApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlyphBarComposer"
        android:usesCleartextTraffic="false">

        <meta-data
            android:name="NothingKey"
            android:value="test" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GlyphBarComposer">

            <!-- App launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                DIRECT APP-TO-APP SHARE (ACTION_SEND):
                When your app shares directly to another instance of itself via the share sheet,
                Android delivers ACTION_SEND with the custom MIME. This handles that case.
            -->
            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.glyphbar.sequence" />
            </intent-filter>

            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.glyphbar.studio" />
            </intent-filter>

            <!--
                OPEN FILE (ACTION_VIEW) — custom MIME type.
                Used when the file is opened from a content URI with the correct MIME.
            -->
            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/vnd.glyphbar.sequence" />
            </intent-filter>

            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/vnd.glyphbar.studio" />
            </intent-filter>

            <!--
                OPEN FILE (ACTION_VIEW) — octet-stream fallback.
                WhatsApp, Telegram, and Gmail all re-label unknown files as
                application/octet-stream. We catch those here.
                Note: pathPattern cannot filter by extension when MIME is octet-stream
                because the system picks MIME before path. We accept all octet-stream
                content URIs and detect the type from JSON content inside MainActivity.
            -->
            <intent-filter android:label="GB4Aura" android:priority="500">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/octet-stream" />
            </intent-filter>

            <!--
                ACTION_SEND fallback for octet-stream (e.g. forwarded via WhatsApp).
            -->
            <intent-filter android:label="GB4Aura" android:priority="500">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

            <!--
                FILE MANAGER fallback — content scheme with .glyph / .gstudio extension.
                Each scheme gets its OWN intent-filter to avoid Android merging them incorrectly.
            -->
            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:scheme="content" android:mimeType="*/*" android:pathPattern=".*\\.glyph" />
            </intent-filter>

            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:scheme="content" android:mimeType="*/*" android:pathPattern=".*\\.gstudio" />
            </intent-filter>

            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:scheme="file" android:mimeType="*/*" android:pathPattern=".*\\.glyph" />
            </intent-filter>

            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:scheme="file" android:mimeType="*/*" android:pathPattern=".*\\.gstudio" />
            </intent-filter>

            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
            </intent-filter>

            <!-- Open from file manager / email / Drive as application/json -->
            <intent-filter android:label="GB4Aura" android:priority="1000">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/json" />
            </intent-filter>


        </activity>

        <service
            android:name=".service.BatteryService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Battery monitoring for Glyph lights" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>